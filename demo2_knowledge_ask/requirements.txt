# Core dependencies for Demo 2 (end-to-end /knowledge/ask)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Same data + embedding stack as Demo 1.
psycopg2-binary>=2.9
pgvector>=0.2.5
numpy>=1.24
python-dotenv>=1.0
fastembed>=0.3.0

# Imported at module load; only actually used when a provider is set to "openai".
openai>=1.0
