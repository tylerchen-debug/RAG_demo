# Core dependencies for Demo 1 (offline ingestion + vector search)
psycopg2-binary>=2.9
pgvector>=0.2.5
numpy>=1.24
python-dotenv>=1.0

# Default embedding provider: fastembed (local, ONNX-based, no API key needed).
fastembed>=0.3.0

# Optional embedding provider. Uncomment if you set EMBEDDING_PROVIDER=openai.
# openai>=1.0
